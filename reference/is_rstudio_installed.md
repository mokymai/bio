# Check whether RStudio Desktop is installed, even if it is not running

Check whether RStudio Desktop is installed, even if it is not running

## Usage

``` r
is_rstudio_installed()
```

## Value

`TRUE`/`FALSE`.
