% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/programs.R
\name{get_rstudio_install_scope}
\alias{get_rstudio_install_scope}
\title{Determine whether the local RStudio Desktop install is system-wide or per-user}
\usage{
get_rstudio_install_scope(install_dir = find_rstudio_install_dir())
}
\arguments{
\item{install_dir}{Character scalar, the resolved RStudio install
directory. Defaults to \code{\link[=find_rstudio_install_dir]{find_rstudio_install_dir()}}.}
}
\value{
\code{"system"}, \code{"user"}, or \code{NA_character_} if the install location
is unknown (e.g. RStudio isn't installed).
}
\description{
RStudio Desktop's Windows installer supports an "all users" (system-wide,
admin rights required) and a "just me" (per-user) install mode; macOS
installs are similarly either shared (\verb{/Applications}) or per-user
(\verb{~/Applications}). This distinction affects only where the RStudio
\emph{application files} live (used e.g. to find the bundled
\code{user-prefs-schema.json}) — it does \strong{not} affect where RStudio stores
preferences, keybindings, or other per-user state, which always lives
under the current OS user's profile regardless of install scope (see
\code{\link[=get_path_rstudio_config_dir]{get_path_rstudio_config_dir()}}).
}
\examples{
if (interactive()) {
  get_rstudio_install_scope()
}
}
