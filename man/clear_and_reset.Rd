% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/settings.R
\name{rstudio_configure_defaults}
\alias{rstudio_configure_defaults}
\alias{rstudio_reset_session_state}
\alias{rstudio_reset_gmc}
\alias{clear_and_reset}
\alias{clear_r_history}
\title{Install classroom/lab default configuration for RStudio and R.}
\usage{
rstudio_configure_defaults(force_update_dictionaries = FALSE)

rstudio_reset_session_state(...)

rstudio_reset_gmc(..., force_update_dictionaries = FALSE)

clear_r_history(backup = TRUE)
}
\arguments{
\item{force_update_dictionaries}{Logical scalar. If \code{TRUE}, the dictionaries
are refreshed even when the current locale is present.}

\item{...}{Further arguments used by \code{restriction_status()} for compatibility.}

\item{backup}{Logical scalar. If \code{TRUE}, save a timestamped backup before
clearing the active history.}
}
\value{
Invisibly returns a data frame with one row per step, its \code{ok}
status, and an error \code{message} (if any).

Invisibly returns a data frame with one row per step, its \code{ok}
status, and an error \code{message} (if any).

Invisibly returns a list with \code{configure} and \code{session_state}
summary data frames.

Invisibly returns \code{NULL}.
}
\description{
Installs the "bio-default" preferences, keybindings and snippets, updates
spellcheck dictionaries and TinyTeX, and (re)creates the course working
directories. Unlike \code{\link[=rstudio_reset_session_state]{rstudio_reset_session_state()}}, every step here is
file-based (settings/keybindings are written straight to disk, see
\code{rstudio_set_preferences()}) or a plain package install. Steps that do need
a live RStudio session (such as
downloading dictionaries via \code{.rs.downloadAllDictionaries()}, or applying
a theme) are attempted but simply reported as failed/skipped when run
headlessly, without stopping the remaining steps.

Clears everything that only exists while RStudio is running: open tabs
(files/plots/help/viewer/projects/terminals/documents), the R workspace,
pane layout and zoom, theme, and — as the very last steps — the R console
and the command history. This function needs a live RStudio session
(it is built entirely on \code{\link[rstudioapi:executeCommand]{rstudioapi::executeCommand()}}).

A thin wrapper that runs \code{\link[=rstudio_configure_defaults]{rstudio_configure_defaults()}} (file-based
configuration, works even outside RStudio) followed by
\code{\link[=rstudio_reset_session_state]{rstudio_reset_session_state()}} (runtime state, requires a live RStudio
session), kept for internal backward compatibility.

Clear and Reset R and RStudio settings and preferences.

Uses \code{\link[utils:savehistory]{utils::savehistory()}} and \code{\link[utils:loadhistory]{utils::loadhistory()}} to back up and clear
the R command history. On Windows in a running RStudio session, RStudio may
manage the active history independently; this helper's effect is therefore
not confirmed for that combination and should be verified manually before
relying on it in a reset workflow.
}
\details{
Every step is run through \code{\link[=run_reset_step]{run_reset_step()}}: a failure in one step is
reported but does not prevent the remaining steps from running, and none
of the steps open interactive confirmation popups.

Every step is run through \code{\link[=run_reset_step]{run_reset_step()}}, so a failure in one step is
reported but does not prevent the remaining steps from running, and none
of the steps open interactive confirmation popups.

This helper is intentionally conservative and protects destructive reset
actions behind a simple override flag (see \code{restriction_status()}). It is
meant to be run on classroom/lab computers, never on a developer's own,
non-standardized RStudio session.
}
\concept{r and rstudio settings}
\keyword{internal}
