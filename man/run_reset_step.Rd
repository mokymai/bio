% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/settings.R
\name{run_reset_step}
\alias{run_reset_step}
\title{Run one reset step without letting it stop the rest of the workflow.}
\usage{
run_reset_step(label, expr)
}
\arguments{
\item{label}{Character scalar describing the step, used in progress output.}

\item{expr}{Expression to evaluate (wrap multiple statements in \code{{ }}).}
}
\value{
Invisibly returns a list with \code{label}, \code{ok} (logical), and
\code{message} (the error message, or \code{NA} on success).
}
\description{
Evaluates \code{expr}, reports success or failure to the console, and turns
warnings into non-fatal notices. Used by \code{\link[=rstudio_reset_gmc]{rstudio_reset_gmc()}} so a single
failing step (e.g. no network for dictionaries) never blocks later steps
and never fails silently.
}
\examples{
bio:::run_reset_step("A step that works", 1 + 1)
bio:::run_reset_step("A step that fails", stop("boom"))
}
\keyword{internal}
